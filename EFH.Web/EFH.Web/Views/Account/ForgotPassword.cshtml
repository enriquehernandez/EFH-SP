@model EFH.Web.Models.ForgotPasswordModelEFH
@{
    ViewBag.Title = "Olvido su clave";
}

<h2>Olvido su clave</h2>

<p class="message-success">@ViewBag.StatusMessage</p>
@*<span style="color:red;">@TempData["Message"]</span>*@

@using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <fieldset>
        @*<legend>Forgot Password Form</legend>*@
        @*<ol>
            <li>
                @Html.Label("User Name", new { @for = "UserName" })
                @Html.TextBox("UserName")
                <span style="color:red;">@TempData["Message"]</span>
            </li>
        </ol>
        <input type="submit" value="Recover" />*@
        <div class="control-group">
            @Html.LabelFor(m => m.Email, new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(m => m.Email)
                @Html.ValidationMessageFor(m => m.Email)
            </div>
        </div>
        <div class="control-group">
            <div class="controls">
                <input type="submit" value="Recuperar" />
            </div>
        </div>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
